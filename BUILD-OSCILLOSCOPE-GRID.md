# Corrected graph-grid build

This commit intentionally triggers a clean Release-1.0 build after commit `72a7bc6`.
Both Gain History and Oscilloscope use the same unconditional parabolic cylinder-grid geometry.
